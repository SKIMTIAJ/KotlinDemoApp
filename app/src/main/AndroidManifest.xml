<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.imtiaj.kotlindemoapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinDemoApp">
        <activity android:name=".feedback.FeedBackActivity">

        </activity>
        <activity android:name=".statics.StaticsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".profile.ProfileActivity"></activity>
        <activity android:name=".totalvisited.TotalVisitedActivity" />
        <activity android:name=".notification.NotificationActivity" />
        <activity android:name=".booking.BookingDetailsActivity" />
        <activity android:name=".home.HomeActivity" />
    </application>

</manifest>